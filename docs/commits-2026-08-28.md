# Commits para subir al repo — 28/08/2026

8 commits, sin superposición de archivos: cada archivo aparece una sola vez, así
podés pegar bloque por bloque sin `git add -p`.

Antes de empezar, chequeo rápido:

```bash
git status --short
```

Tienen que aparecer 26 modificados y 6 nuevos (4 docs + `actionShape.js`,
`anchorLinks.js`, `mobilityActions.js`). Si aparece `drive-files/`, frená: ya
está en `.gitignore`, revisá que no lo hayas forzado con `git add -f`.

---

## 1 · Universo canónico de acciones

```bash
git add src/demo/data/actionShape.js \
        src/demo/data/mobilityActions.js \
        src/demo/data/actions.js \
        src/demo/data/impact.js \
        src/demo/data/reports.js \
        src/demo/data/dataRoom.js

git commit -m "data: unifica las 14 acciones del nodo en un sobre común" -m "Energía, movilidad y recuperación de plástico compartían nodo pero no
estructura: cada módulo leía campos distintos y el listado sólo mostraba
las 8 de energía.

- actionShape.js: enums compartidos (estado, paso, tipo de acción). Existe
  para cortar el ciclo de imports entre actions.js y el adaptador de
  movilidad.
- mobilityActions.js: adapta los 5 viajes de Strava al sobre común. No
  recalcula nada, sólo mapea lo que ya viene del sync.
- actions.js: las 8 acciones de energía se reconstruyen desde la fuente
  (totalKwh, periodDays, reductionPercent, sesDelta real, platformActionId).
  Se agrega la Botella de Amor. NODE_ACTIONS = energía + movilidad.
- Cierra la reconciliación de SES contra node_state: energía 20 + Botella de
  Amor 3 + movilidad 12 = 35. Dos valores derivados se corrigieron contra la
  fuente (18-dic: -10,565867% / SES 0 · 20-ene: +37,992455% / SES -30).
- MISSING_ACTION_PACKAGES queda vacío: no falta ningún paquete."
```

## 2 · Anclajes on-chain

```bash
git add src/demo/data/anchorLinks.js

git commit -m "data: arma los enlaces a IPFS y al explorador desde el anclaje" -m "proofState() distingue tres situaciones que antes se aplanaban en
'sin anclar': anchored, tx_unconfirmed y pending. El estado intermedio es
el que describió Martín — la transacción existe y se puede abrir, lo que
todavía no llegó al Sync es la confirmación enriquecida (bloque y
timestamp).

Cuando no hay CID o no hay hash, no se fabrica un link: se devuelve null y
la pantalla muestra el estado."
```

## 3 · Estados de registro del piloto Montessori

```bash
git add src/demo/data/montessori/index.js

git commit -m "montessori: cuatro estados de registro y alcance explícito del piloto" -m "recordStateOf() clasifica cada registro en lugar de mostrarlo como
completo o vacío. PILOT_SCOPE queda vacío a propósito: mientras no esté
definido el alcance, outOfScopeReason() devuelve el motivo y la pantalla
lo dice, en vez de rellenar con un valor estimado."
```

## 4 · Dashboard sobre el sobre común

```bash
git add src/demo/modules/ActionDetail.jsx \
        src/demo/modules/Auditoria.jsx \
        src/demo/modules/Timeline.jsx \
        src/demo/modules/MisAcciones.jsx \
        src/demo/modules/Reportes.jsx \
        src/demo/modules/Identity.jsx \
        src/demo/DemoHub.jsx \
        src/demo/components/StatusChip.jsx

git commit -m "dashboard: las fichas leen metric/outcome y los anclajes son navegables" -m "- ActionDetail leía consumption/result, campos que sólo existen en
  energía. Abrir act_martin_plastico_01 tiraba 'Cannot read properties of
  undefined' y dejaba la pantalla en blanco. Ahora lee el sobre común, y
  los ids de viaje redirigen al módulo Movilidad en lugar de renderizar
  una ficha de energía a medio llenar.
- Los pasos CID y Blockchain de la cadena de trazabilidad son enlaces
  reales al gateway de IPFS y a BscScan, con target=_blank.
- Auditoría: ruteo por acción, celdas de CID y Blockchain enlazadas,
  columna de estado de registro con su referencia.
- Identity: la reconciliación de SES cierra en 35.
- DeltaPct devuelve 'No aplica' cuando el valor es null, en vez de 0%."
```

## 5 · Contraste y clicks

```bash
git add src/styles/tokens.css \
        src/demo/demo.css \
        src/demo/data/nodeTypes.js \
        src/demo/components/DashSidebar.jsx

git commit -m "ui: define --ink-100 y libera los clicks sobre la fila" -m "Dos bugs de CSS que se veían como uno solo.

1. --ink-100 no estaba definido y demo.css lo usa 12 veces sin fallback.
   Una declaración con var() indefinida se descarta entera, así que el
   texto heredaba el color del <button> contenedor: casi negro sobre fondo
   oscuro. Sólo se leía en hover, que sí aplicaba un color existente.
   Definido en #C2D3E8 — 12:1 de contraste sobre la tarjeta.

2. El overlay invisible de la fila (::after con inset:0) se comía los
   clicks de 'Ver en IPFS' y 'Ver en BscScan'. Los enlaces y botones que
   no son el rowlink pasan a position:relative + z-index:1.

También: label 'Identidad Ambiental' y logo transparente en la sidebar."
```

## 6 · Dominio y og-image

```bash
git add index.html \
        src/components/Footer.jsx \
        src/components/Contact.jsx \
        public/og-image.png

git commit -m "web: sustainprotocol.org y og-image nueva" -m "7 referencias apuntaban todavía a sustaintoken.org, incluida og:image.
La imagen vieja seguía en public/ y Vite copia public/ tal cual, por eso
la build la reponía en cada dist.

Pendiente menor: la PNG pesa 499 KB. Conviene reexportarla desde el
diseño original (~60 KB) antes de que la lea un crawler."
```

## 7 · Verificación

```bash
git add scripts/verify-canonical.mjs scripts/smoke.mjs

git commit -m "verify: invariantes del universo canónico y smoke sobre todas las rutas" -m "- verify-canonical: ~17 invariantes nuevas — 14 acciones, los tramos de
  SES suman 35, forma y unicidad de cada anclaje, los cuatro estados de
  registro.
- smoke: las rutas de detalle se derivan de NODE_ACTIONS (40 -> 54). Antes
  probaba una acción hardcodeada, por eso la pantalla en blanco del
  plástico pasó el smoke.
- Guarda de tokens CSS: falla si demo.css usa una variable sin definir y
  sin fallback. Probada en negativo contra el bug de --ink-100."
```

## 8 · Documentación

```bash
git add docs/anclajes-energia-2026-08-28.md \
        docs/borrador-respuesta-martin-2026-08-24.md \
        docs/data-room-drive-2026-08-28.md \
        docs/mensajes-martin-2026-08-25.md \
        docs/transcripcion-audios-2026-08-28.md \
        docs/resumen-cliente.md \
        docs/commits-2026-08-28.md

git commit -m "docs: transcripciones, anclajes y estado del cliente al 28/08" -m "- transcripcion-audios-2026-08-28: los dos audios de WhatsApp.
- anclajes-energia-2026-08-28: los 8 pares CID/tx de las facturas de
  Martín, con la corrección de que sí existían — lo que no teníamos eran
  las pruebas, no los anclajes.
- data-room-drive-2026-08-28: qué hay en el Data Room del protocolo,
  incluidos los dos nodos que no incorporamos por 'no ampliar alcance'.
- mensajes-martin-2026-08-25 y borrador-respuesta-martin-2026-08-24: sin
  aprobar todavía, no salen al cliente.
- resumen-cliente reescrito.

Las 8 facturas de EDESUR son de Martín, no del Colegio Montessori."
```

---

## Después

```bash
git log --oneline -8
npm run verify:canonical && npm run verify:attribution && npm run smoke
git push
```

Los tres scripts tienen que pasar antes del push. Si `smoke` falla en una
ruta de detalle, revisá que el archivo de datos de esa acción tenga el sobre
completo (`kind`, `metric`, `outcome`, `baseline`, `ses`, `mrv`, `anchor`,
`dataRoom`, `detailPath`).
